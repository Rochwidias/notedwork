## Task 1: Create NotesView component

**Files:**
- Create: `components/NotesView.tsx`
- Modify: `components/AppNav.tsx` (add button linking to `/notes`)
- Test: `tests/components/NotesView.test.tsx`

**Interfaces:**
- Consumes: none (stand‑alone view).
- Produces: `Note` type defined in `lib/types.ts`.

**Steps (to implement):**
1. Write failing test for empty state.
2. Implement minimal component with empty state message.
3. Write test for rendering note cards (later tasks will add notes).
4. Ensure component uses existing design tokens.
5. Commit changes.
